
    // const resizedDetections = faceapi.resizeResults(detections, displaySize)
    // var context = canvas.getContext("2d");
    // canvas.getContext('2d').clearRect(0, 0, canvas.width, canvas.height)

    // const x1 = resizedDetections[0].landmarks.positions[67].x
    // const y1 = resizedDetections[0].landmarks.positions[67].y

    // const x2 = resizedDetections[0].landmarks.positions[66].x
    // const y2 = resizedDetections[0].landmarks.positions[66].y

    // const x3 = resizedDetections[0].landmarks.positions[65].x
    // const y3 = resizedDetections[0].landmarks.positions[65].y

    // const x4 = resizedDetections[0].landmarks.positions[64].x
    // const y4 = resizedDetections[0].landmarks.positions[64].y

    // const x5 = resizedDetections[0].landmarks.positions[63].x
    // const y5 = resizedDetections[0].landmarks.positions[63].y

    // const x6 = resizedDetections[0].landmarks.positions[62].x
    // const y6 = resizedDetections[0].landmarks.positions[62].y

    // const x7 = resizedDetections[0].landmarks.positions[61].x
    // const y7 = resizedDetections[0].landmarks.positions[61].y

    // const x8 = resizedDetections[0].landmarks.positions[60].x
    // const y8 = resizedDetections[0].landmarks.positions[60].y 

    // context.beginPath();
    // context.moveTo(x1, y1);
    // context.lineTo(x2, y2);
    // context.strokeStyle = "yellow"; // Couleur de la ligne
    // context.lineWidth = 3; // Épaisseur de la ligne
    // context.stroke();

    // context.beginPath();
    // context.moveTo(x2, y2);
    // context.lineTo(x3, y3);
    // context.strokeStyle = "yellow"; // Couleur de la ligne
    // context.lineWidth = 3; // Épaisseur de la ligne
    // context.stroke();

    // context.beginPath();
    // context.moveTo(x3, y3);
    // context.lineTo(x4, y4);
    // context.strokeStyle = "yellow"; // Couleur de la ligne
    // context.lineWidth = 3; // Épaisseur de la ligne
    // context.stroke();

    // context.beginPath();
    // context.moveTo(x4, y4);
    // context.lineTo(x5, y5);
    // context.strokeStyle = "yellow"; // Couleur de la ligne
    // context.lineWidth = 3; // Épaisseur de la ligne
    // context.stroke();

    // context.beginPath();
    // context.moveTo(x5, y5);
    // context.lineTo(x6, y6);
    // context.strokeStyle = "yellow"; // Couleur de la ligne
    // context.lineWidth = 3; // Épaisseur de la ligne
    // context.stroke();
      
    // context.beginPath();
    // context.moveTo(x6, y6);
    // context.lineTo(x7, y7);
    // context.strokeStyle = "yellow"; // Couleur de la ligne
    // context.lineWidth = 3; // Épaisseur de la ligne
    // context.stroke();

    // context.beginPath();
    // context.moveTo(x7, y7);
    // context.lineTo(x8, y8);
    // context.strokeStyle = "yellow"; // Couleur de la ligne
    // context.lineWidth = 3; // Épaisseur de la ligne
    // context.stroke();

    // context.beginPath();
    // context.moveTo(x8, y8);
    // context.lineTo(x1, y1);
    // context.strokeStyle = "yellow"; // Couleur de la ligne
    // context.lineWidth = 3; // Épaisseur de la ligne
    // context.stroke();


    // faceapi.draw.drawDetections(canvas, resizedDetections)
    // faceapi.draw.drawFaceLandmarks(canvas, resizedDetections)
    // faceapi.draw.drawFaceExpressions(canvas, resizedDetections)